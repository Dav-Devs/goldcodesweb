// $(document).ready(() => {

// /*     var options = {
//         focus: true,
//     };

// var logout_modal = new bootstrap.Modal($("#logout_modal"), options) */

// var lmel = $("#logout_modal");
// var logout_modal = bootstrap.Modal.getOrCreateInstance(lmel); // Returns a Bootstrap modal instance

// });